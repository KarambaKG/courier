<h1>Назначение курьера</h1>
<%= form_for @order do |f| %>

    <div>
      <%= f.label :order_user_id %> <br>
      <%= f.collection_select :order_user_id, User.all, :id, :name %>
    </div>

    <div><%= f.submit %></div>
<% end %>